module.exports = {
  // 最大长度100个字符
  printWidth: 100,
  // 行末分号 
  semi: true,
  // 缩进
  tabWidth: 2,
  // 使用tab还是空格
  useTabs: false,
  // 单引号
  singleQuote: false,
  // 尽可能使用尾随逗号（包括函数参数）
  trailingComma: "none",
  // 大括号内的首尾需要空格
  bracketSpacing: true,
  // 箭头函数圆括号
  arrowParens: "avoid",
  // JSX双引号
  jsxSingleQuote: false,
  // > 标签放在最后一行的末尾，而不是单独放在下一行
  jsxBracketSameLine: false,
};